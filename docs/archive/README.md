# docs/archive 说明

`docs/archive/` 用于存放已经脱离日常协作主链的方案、评审和历史说明文档。

规则：

- 已归档文档可以保留历史原名，不做追溯重命名。
- 新增归档文档使用小写蛇形命名；同主题追加版本时使用 `_v2`、`_v3` 后缀。
- 活跃任务过程文档仍放在 `docs/tasks/<task-id>/`，不要把正在推进的任务移动到本目录。
- 一次性评审、阶段性总结、过期设计草案优先归档到这里。
