# 2026-03 工作日志

## 2026-03-02（fix-integration-and-branch-consolidation-v1）

### 本次目标

- 一次性修复当前分支门禁阻断项。
- 完成关键体验文案修复（战斗结束中文化）。
- 将修复分支统筹合并到 `main`，并执行激进分支清理（带归档标签）。

### 主要改动

- 修复类型安全：`EncounterRegistry.get_encounter_by_id` 改为显式 `Variant` 判定后返回 `Dictionary`。
- 去除场景层动态调用：`app.gd` 路由分发由动态回调改为静态 `match`，并将 `before_attach` 由 `Callable` 改为动作枚举。
- 降低模块对场景类型依赖：
  - `relic_potion` 相关服务移除 `Player/Enemy` 直接类型约束，改为 `Node` + 有效性检查。
  - `EnemySpawnService` 改为由场景层注入 `enemy_scene`，移除模块内 `runtime/scenes` 路径依赖。
- 修复门禁脚本最小例外：`dynamic_call_guard_check.sh` 补齐必要回调文件 allowlist。
- 战斗结束文案改为中文：`胜利！` / `游戏结束！`。
- 新建任务三件套：`docs/tasks/fix-integration-and-branch-consolidation-v1/`。

### 验证

- [x] `make workflow-check TASK_ID=fix-encounter-and-battle-potion-gating-v1` 通过
- [x] `make test` 通过（165/165）

### 分支治理

- 在删除前为所有本地非 `main` 分支创建归档标签：`archive/<branch>-20260302`。
- 本次共创建 `31` 个归档标签，覆盖清理前全部本地非 `main` 分支。
- 完成激进清理后，本地仅保留 `main` 分支。

## 2026-03-02（fix-p1-tooltip-and-death-race-v1）

### 本次目标

- 修复遗物悬停 tooltip 缺失。
- 将死亡处理竞态彻底收口到战斗场景层。

### 主要改动

- `RelicPotionUI`：
  - 为遗物/药水按钮增加 `tooltip_text` 兜底；
  - 增加按钮横向填充与手型指针，提升悬停可发现性；
  - 保留 `Events.*_tooltip_requested` 自定义 tooltip 事件链。
- `Player` / `Enemy`：
  - 去掉 `take_damage` 中的 `queue_free()`；
  - 新增 `_death_notified` 防重入，仅发死亡信号，由 battle 层统一回收节点。
- 新增回归测试（3 条）：
  - `test_relic_ui_emits_tooltip_on_hover`
  - `test_player_take_damage_does_not_queue_free_self`
  - `test_enemy_take_damage_does_not_queue_free_self`

### 验证

- [x] `make workflow-check TASK_ID=fix-p1-tooltip-and-death-race-v1` 通过
- [x] `make test` 通过（168/168）

## 2026-03-06（fix-head-stability-and-potion-json-truth）

### 本次目标

- 修复 2026-03-06 新增改动引入的 headless 编译回归与门禁阻断。
- 将药水运行时真源从 `.tres` 收口到 JSON 管线。
- 补齐负路径门禁，并同步文档口径。

### 主要改动

- 编译稳定性：
  - 将 `UIColors`、`UILayout`、`IntentActionData`、`HandZonePort`、`CombatantRole` 的跨文件使用改为显式 `preload`；
  - 清理 `Events/*Events` 对场景层 `class_name` 的信号类型依赖；
  - 为 GUT 运行脚本补充 `--import` 预热，并新增 `test_compile_regression_smoke.gd`。
- 战斗接线与场景/模块解耦：
  - `app.gd` 改为显式向 battle scene 注入 `runtime_stats`、`encounter_id`、`relic_potion_system`；
  - `EnemySpawnService` 移除模块层对 `Enemy` 场景类型的直接依赖；
  - `EnemyActionPicker` 去掉对 `player` group 的全局查找，RNG key 改为稳定敌人槽位签名。
- 药水真源收口：
  - `PotionCatalog` 改为直接读取 `base_potions.json` 并构造内存态 `PotionData`；
  - `content_import_potions.py` 对齐 `potion_schema.json`，补充未知字段与非法 `art_path` 校验；
  - `test_content_pipeline_catalogs.gd` 改为校验药水池与 JSON 源数据一致。
- 测试/健壮性补强：
  - `CharacterStats.create_instance()` 兼容空 `starting_deck`；
  - 回补 `BuffSystem._trigger_poison()` 测试入口并修正敌人死亡回收保护；
  - 修正 `test_game_effect_executor.gd` 中 callable 捕获方式，避免闭包误判。

### 验证

- [x] `make ci-check` 通过
- [x] `bash dev/tools/content_pipeline_check.sh` 通过
- [x] `make test` 通过（285/285）
- [x] `make test-effects-matrix` 通过
