# 2026-03 工作日志

## 2026-03-02（fix-integration-and-branch-consolidation-v1）

### 本次目标

- 一次性修复当前分支门禁阻断项。
- 完成关键体验文案修复（战斗结束中文化）。
- 将修复分支统筹合并到 `main`，并执行激进分支清理（带归档标签）。

### 主要改动

- 修复类型安全：`EncounterRegistry.get_encounter_by_id` 改为显式 `Variant` 判定后返回 `Dictionary`。
- 去除场景层动态调用：`app.gd` 路由分发由动态回调改为静态 `match`，并将 `before_attach` 由 `Callable` 改为动作枚举。
- 降低模块对场景类型依赖：
  - `relic_potion` 相关服务移除 `Player/Enemy` 直接类型约束，改为 `Node` + 有效性检查。
  - `EnemySpawnService` 改为由场景层注入 `enemy_scene`，移除模块内 `runtime/scenes` 路径依赖。
- 修复门禁脚本最小例外：`dynamic_call_guard_check.sh` 补齐必要回调文件 allowlist。
- 战斗结束文案改为中文：`胜利！` / `游戏结束！`。
- 新建任务三件套：`docs/tasks/fix-integration-and-branch-consolidation-v1/`。

### 验证

- [x] `make workflow-check TASK_ID=fix-encounter-and-battle-potion-gating-v1` 通过
- [x] `make test` 通过（165/165）

### 分支治理

- 在删除前为所有本地非 `main` 分支创建归档标签：`archive/<branch>-20260302`。
- 本次共创建 `31` 个归档标签，覆盖清理前全部本地非 `main` 分支。
- 完成激进清理后，本地仅保留 `main` 分支。

## 2026-03-02（fix-p1-tooltip-and-death-race-v1）

### 本次目标

- 修复遗物悬停 tooltip 缺失。
- 将死亡处理竞态彻底收口到战斗场景层。

### 主要改动

- `RelicPotionUI`：
  - 为遗物/药水按钮增加 `tooltip_text` 兜底；
  - 增加按钮横向填充与手型指针，提升悬停可发现性；
  - 保留 `Events.*_tooltip_requested` 自定义 tooltip 事件链。
- `Player` / `Enemy`：
  - 去掉 `take_damage` 中的 `queue_free()`；
  - 新增 `_death_notified` 防重入，仅发死亡信号，由 battle 层统一回收节点。
- 新增回归测试（3 条）：
  - `test_relic_ui_emits_tooltip_on_hover`
  - `test_player_take_damage_does_not_queue_free_self`
  - `test_enemy_take_damage_does_not_queue_free_self`

### 验证

- [x] `make workflow-check TASK_ID=fix-p1-tooltip-and-death-race-v1` 通过
- [x] `make test` 通过（168/168）

## 2026-03-06（fix-stability-hardening-v1）

### 本次目标

- 将当前主干从“能跑但不稳”收口到“本地与远端都可验证”。
- 修复当前门禁/测试基础链路阻断项。
- 收敛初始化链、DOT 死亡链路、BuffSystem 兼容与参与者解析回归。
- 同步进度文档、路线图与任务三件套。

### 主要改动

- 门禁与测试链路：
  - `app.gd` 改为显式注入 `runtime_stats / encounter_id / relic_potion_system`，移除 battle 场景动态 `call` 初始化；
  - 新增 `dev/tools/ensure_godot_import.sh`，本地测试脚本支持 import 预热与缓存戳记；
  - GitHub Actions 安装 Godot 4.5.1，并执行 import + `ci-check` + `make test`。
- 运行态兼容与回归：
  - `CharacterStats.create_instance`、`RunState.init_with_character` 增加空资源容忍；
  - `battle.gd` 增加敌人回收空父节点保护；
  - `BuffSystem` 补 `_trigger_poison` / `_trigger_burn` 兼容入口，保持注册式架构不回退；
  - `EnemySpawnService` 去除模块对 `Enemy` 场景类型的依赖；
  - `BattleParticipantResolver` 回退为优先选择当前有效 `player` 组节点；
  - 修正 `GameEffectExecutor` 的 draw callable 路径，并同步更新对应单测闭包写法。
- 流程与文档：
  - 更新 `docs/session/progress.md` 与 `docs/roadmap/README.md`，使“当前基线”与仓库真实状态一致；
  - 新增任务三件套：`docs/tasks/fix-stability-hardening-v1/`。

### 验证

- [x] `make ci-check` 通过
- [x] `make test` 通过（30 scripts / 284 tests / 284 passing）
- [x] 聚类回归通过：
  - `res://dev/tests/integration/test_battle_flow.gd`
  - `res://dev/tests/unit/test_game_effect_executor.gd`
  - `res://dev/tests/unit/test_run_state_deserializer.gd`
  - `res://dev/tests/unit/test_runtime_contract_guards.gd`
- [x] 干净副本下 `bash dev/tools/run_gut_test_file.sh res://dev/tests/test_gut_smoke.gd 120` 自动 import 后通过

### 已知保留项

- `test_full_run_autoplay` / `test_run_flow` 仍会暴露 encounter coverage warning（`floor=5 elite`、`floor=12 common`）；本轮保留为 warning，不扩展到内容表深清理。
- 测试尾部仍存在既有 orphan / resource leak 告警；本轮以“不新增、不升级为失败”为准。
