# 2026-02 工作日志

## 2026-02-15

- 初始化协作脚手架：补齐 `docs/module_architecture.md`、模板文档、契约目录。
- 新增 `Makefile` 与 `tools` 脚本，支持 `make content-index`、`make workflow-check`、`make install-hooks`、`make new-task`。
- 校验并接入教程基线目录：`references/tutorial_baseline`。
- 迁移参考资料到 `references/slay_the_spire_cn`，并更新索引脚本路径。
- 新增模块缺口分析与 V2 开发路线图，包含资源全量替换任务拆解。

## 2026-02-16

- 完成 Phase 1「架构收口」文档交付：新增 `docs/contracts/module_boundaries_v1.md`。
- 回填 `docs/module_architecture.md`，按真实代码依赖重写模块总览与依赖方向。
- 回填 `docs/repo_structure.md`，明确“当前结构 + 目标结构 + 迁移原则”。
- 新增任务三件套：`docs/tasks/phase1-architecture-closure-v1/{plan,handoff,verification}.md`。
- 更新模块说明文档：`modules/README.md`、`modules/run_meta/README.md`、`modules/run_flow/README.md`、`modules/save_seed_replay/README.md`、`modules/ui_shell/README.md`。
- 明确命名收口建议：`persistence` 为存档主模块，`save_seed_replay` 冻结为占位，`run_flow` 作为后续应用编排归属目录。

## 2026-02-16（Phase 2 第一批）

- 新增 `run_flow` 应用服务骨架：`run_flow_service.gd` + `shop/event/rest` 命令服务。
- 改造 `scenes/shop/shop_screen.gd`：购买/删卡/离开不再直接写 `RunState`，改由 `ShopFlowService` 执行。
- 改造 `scenes/events/event_screen.gd`：事件选项应用与继续推进改由 `EventFlowService` 执行。
- 改造 `scenes/map/rest_screen.gd`：休息/强化写状态改由 `RestFlowService` 执行。
- 在 `scenes/app/app.gd` 注入 `RunFlowService` 并向页面下发子服务。
- 更新 `docs/contracts/module_boundaries_v1.md`、`docs/module_architecture.md`、`docs/repo_structure.md` 与当前实现对齐。
- 新增任务三件套：`docs/tasks/phase2-scene-decoupling-v1/{plan,handoff,verification}.md`。
- 修复联调崩溃：`scenes/ui/stats_ui.gd` 将 `Label.theme_override_font_sizes` 非法访问改为 `add_theme_font_size_override(\"font_size\", 18)`。

## 2026-02-16（Phase 3：battle flow decoupling v1）

- 新增 `modules/run_flow/battle_flow_service.gd`，将 battle 完成后的路由判定与奖励应用收口到应用服务层。
- 更新 `modules/run_flow/run_flow_service.gd`，接入 `battle_flow_service` 聚合注入。
- 改造 `scenes/app/app.gd`：迁移 battle 结束分支（胜利/失败）与奖励应用到 run_flow 命令调用，app 保留接线与页面切换。
- 更新 `modules/run_flow/README.md`，补充 battle -> reward -> map 命令返回契约。
- 同步更新 `docs/contracts/module_boundaries_v1.md`、`docs/module_architecture.md`、`docs/repo_structure.md`。
- 新增任务三件套：`docs/tasks/phase3-battle-flow-decoupling-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 4：map orchestration decoupling v1）

- 新增 `modules/run_flow/route_dispatcher.gd`，统一 `next_route + payload` 返回契约与路由常量。
- 新增 `modules/run_flow/map_flow_service.gd`，收口地图节点进入判定、placeholder 节点 `next_floor` 跳转、非战斗节点完成后路由决策。
- 更新 `modules/run_flow/run_flow_service.gd`，聚合注入 `route_dispatcher` 与 `map_flow_service`。
- 更新 `modules/run_flow/battle_flow_service.gd`，改为复用统一路由结果构造器。
- 瘦身 `scenes/app/app.gd`：移除 `enter_map_node` / placeholder / B3 bonus 编排写入，改为命令结果驱动页面实例化与信号接线。
- 更新 `modules/run_flow/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture` / `repo_structure`）。
- 新增任务三件套：`docs/tasks/phase4-map-orchestration-decoupling-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 5：flow context and contract tests v1）

- 新增 `modules/run_flow/flow_context.gd`，承接跨页面流程上下文（`pending_node_type` / `pending_reward_gold`）。
- 更新 `modules/run_flow/run_flow_service.gd`，聚合 `flow_context` 并向 app 暴露上下文读写入口。
- 瘦身 `scenes/app/app.gd`：删除本地 `pending_*` 字段，改为通过 `run_flow_service` 获取流程上下文。
- 更新 `modules/run_flow/battle_flow_service.gd`：删除重复路由常量，统一复用 `RunRouteDispatcher` 常量单点定义。
- 新增最小契约检查脚本 `tools/run_flow_contract_check.sh`，覆盖 map_flow/battle_flow 关键路由与 payload 契约。
- 同步更新 `modules/run_flow/README.md`、`docs/contracts/module_boundaries_v1.md`、`docs/module_architecture.md`、`docs/repo_structure.md`。
- 新增任务三件套：`docs/tasks/phase5-flow-context-and-contract-tests-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 6：ui shell viewmodel decoupling v1）

- 在 `modules/ui_shell` 新增首批壳层实现：
  - `viewmodel/stats_view_model.gd`
  - `viewmodel/relic_potion_view_model.gd`
  - `adapter/stats_ui_adapter.gd`
  - `adapter/relic_potion_ui_adapter.gd`
- 改造 `scenes/ui/stats_ui.gd`：改为读取 `StatsUIAdapter` 投影并渲染，不再直接拼装状态徽章数据。
- 改造 `scenes/ui/relic_potion_ui.gd`：改为读取 `RelicPotionUIAdapter` 投影并通过 adapter 转发“使用药水”命令。
- 同步更新 `modules/ui_shell/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture` / `repo_structure`）。
- 新增任务三件套：`docs/tasks/phase6-ui-shell-viewmodel-decoupling-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 7：quality gates and regression v1）

- 新增 `tools/ui_shell_contract_check.sh`：
  - 拦截 `scenes/ui` 直接调用 `run_state.set_/add_/remove_/clear_/advance_/mark_/apply_`。
  - 校验 `stats_ui`、`relic_potion_ui` 仍通过 adapter/viewmodel 接入。
- 扩展 `tools/run_flow_contract_check.sh`：
  - 新增 `ROUTE_*` 常量单点定义校验（仅允许 `route_dispatcher.gd` 定义）。
  - 扩展 map/battle 关键 `next_route + payload` 键位回归检查。
- 更新 `tools/workflow_check.sh`：
  - 在白名单检查后串行执行 `ui_shell_contract_check.sh` 与 `run_flow_contract_check.sh`，形成提交前必过门禁。
- 同步更新 `modules/ui_shell/README.md`、`modules/run_flow/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture` / `repo_structure`）。
- 新增任务三件套：`docs/tasks/phase7-quality-gates-and-regression-v1/{plan,handoff,verification}.md`。

## 2026-02-17（chore-ai-planner-reviewer-character-prompts-v1）

- 新增协作手册：`docs/ai_planner_reviewer_playbook.md`，固化 Planner + Reviewer 工作流与审核通过门槛。
- 新增工程提示词模板：`docs/prompts/engineering_rpi_prompts.md`，覆盖 Research/Plan/Implement/Self Review/Handoff 五阶段。
- 新增人物提示词套件：`docs/prompts/character_portrait_prompts/20_角色提示词套件_v2.md`，包含霜北刀/离恨烟/埋骨钱的可直接投喂文本、批量生成模板与二次修正模板。
- 更新提示词索引：`docs/prompts/README.md`、`docs/prompts/character_portrait_prompts/README.md`。
- 新增任务三件套：`docs/tasks/chore-ai-planner-reviewer-character-prompts-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 9：seed deterministic draw shuffle v1）

- 在 `content/custom_resources/card_pile.gd` 新增 `shuffle_with_rng(stream_key)`，使用 `RunRng` + Fisher-Yates 实现确定性洗牌。
- 在 `runtime/scenes/player/player_handler.gd` 替换战斗链路洗牌调用：
  - 战斗开始抽牌堆洗牌：`battle_start_shuffle`
  - 弃牌回洗抽牌堆：`reshuffle_discard`
- 更新 `runtime/modules/seed_replay/README.md`，记录确定性洗牌落点与 stream key 约束。
- 新增任务三件套：`docs/tasks/phase9-seed-deterministic-draw-shuffle-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 8：ui shell battle_ui decoupling v1）

- 在 `modules/ui_shell` 新增 `battle_ui` 壳层实现：
  - `viewmodel/battle_ui_view_model.gd`
  - `adapter/battle_ui_adapter.gd`
- 改造 `scenes/ui/battle_ui.gd`：移除对 `CardZonesModel` 的直接依赖，改为通过 `BattleUIAdapter` 获取牌区计数投影并转发"结束回合"命令。
- 扩展 `tools/ui_shell_contract_check.sh`：
  - 新增 `battle_ui` 接线校验（必须通过 adapter/viewmodel）。
  - 新增禁止 `battle_ui.gd` 直接导入 `card_system/card_zones_model` 的检查。
- 同步更新 `modules/ui_shell/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture`）。
- 新增任务三件套：`docs/tasks/phase8-ui-shell-battle-ui-decoupling-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 10：persistence status serialization v1）

- 在 `runtime/modules/persistence/save_service.gd` 新增玩家状态层序列化与恢复：
  - 升级 `SAVE_VERSION` 为 v2，新增 `MIN_COMPAT_VERSION` 为 v1。
  - 在 `_serialize_player_stats` 中新增 `statuses` 字段，调用 `stats.get_status_snapshot()` 获取状态快照。
  - 在 `_apply_player_stats` 中新增状态层恢复逻辑，遍历 `statuses` 字典调用 `stats.set_status()`。
- 实现存档版本向后兼容：
  - v1 存档读取时，`statuses` 字段缺失则使用空字典默认值，不恢复任何状态层。
  - v2 存档读取时，按 `statuses` 字典恢复各状态层数值。
- 同步更新 `docs/contracts/run_state.md`：
  - 新增 `player_stats.statuses` 字段说明。
  - 新增版本兼容策略说明。
  - 新增 `statuses = {}` 默认值说明。
- 同步更新 `runtime/modules/persistence/README.md`：
  - 新增 Phase 10 状态与职责说明。
  - 新增存档版本与兼容策略说明。
- 新增任务三件套：`docs/tasks/phase10-persistence-status-serialization-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 11：run flow app lifecycle decoupling v1）

- 新增 `modules/run_flow/run_lifecycle_service.gd`，收口生命周期逻辑：
  - `start_new_run()`：新局初始化（含 RNG/REPRO 初始化）。
  - `try_load_saved_run()`：读档恢复（含 RNG 状态恢复）。
  - `save_checkpoint()`：checkpoint 存档。
  - `update_repro_progress()` / `log_node_enter()`：复盘日志更新。
- 更新 `modules/run_flow/run_flow_service.gd`，聚合注入 `lifecycle_service`。
- 瘦身 `scenes/app/app.gd`：
  - 移除对 `persistence`、`run_rng`、`repro_log` 的直接依赖。
  - 新局初始化、读档恢复、checkpoint 存档、复盘日志更新改为通过 `run_flow_service.lifecycle_service` 调用。
  - `app.gd` 保持"页面实例化 + 路由分发 + 事件接线"薄层职责。
- 同步更新 `modules/run_flow/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture`）。
- 新增任务三件套：`docs/tasks/phase11-run-flow-app-lifecycle-decoupling-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 12：run flow lifecycle contract gate v1）

- 新增 `dev/tools/run_lifecycle_contract_check.sh`：
  - 禁止 `scenes/app/app.gd` 直接 preload/use `persistence/save_service.gd`、`run_rng.gd`、`repro_log.gd`。
  - 强制 `app.gd` 通过 `run_flow_service.lifecycle_service` 调用 `start_new_run/try_load_saved_run/save_checkpoint`。
  - 目的：防止后续回归把生命周期逻辑再次耦合到入口场景。
- 更新 `dev/tools/workflow_check.sh`，串行纳入 `run_lifecycle_contract_check.sh`。
- 同步更新 `modules/run_flow/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture`）。
- 新增任务三件套：`docs/tasks/phase12-run-flow-lifecycle-contract-gate-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 13：persistence contract gate v1）

- 新增 `dev/tools/persistence_contract_check.sh`：
  - 校验 `SAVE_VERSION` 与 `MIN_COMPAT_VERSION` 常量存在。
  - 校验 `_serialize_player_stats` 包含 `statuses` 字段（来自 `get_status_snapshot`）。
  - 校验 `_apply_player_stats` 包含 `statuses` 恢复逻辑（调用 `set_status`）。
  - 校验读取 `statuses` 时对旧存档有默认空字典兜底（兼容 v1）。
  - 目的：防止后续改动破坏 phase10 的"状态层存档兼容"能力。
- 更新 `dev/tools/workflow_check.sh`，串行纳入 `persistence_contract_check.sh`。
- 同步更新 `modules/persistence/README.md`、`docs/contracts/run_state.md` 与架构文档（`module_boundaries_v1` / `module_architecture`）。
- 新增任务三件套：`docs/tasks/phase13-persistence-contract-gate-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 14：seed rng contract gate v1）

- 新增 `dev/tools/seed_rng_contract_check.sh`：
  - 校验 `card_pile.gd` 存在 `shuffle_with_rng(stream_key)` 方法。
  - 校验 `shuffle_with_rng` 内使用 `RunRng.randi_range`（非系统默认 shuffle）。
  - 校验 `player_handler.gd` 的 `start_battle` 使用 `shuffle_with_rng("battle_start_shuffle")`。
  - 校验 `player_handler.gd` 的 `reshuffle_deck_from_discard` 使用 `shuffle_with_rng("reshuffle_discard")`。
  - 校验 `run_lifecycle_service.gd` 存在 `restore_run_state` 逻辑。
  - 校验 `run_lifecycle_service.gd` 存在 `begin_run` 回退逻辑。
  - 目的：防止后续改动破坏"确定性洗牌 + 读档随机流连续性"约束。
- 更新 `dev/tools/workflow_check.sh`，串行纳入 `seed_rng_contract_check.sh`。
- 同步更新 `modules/seed_replay/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture`）。
- 新增任务三件套：`docs/tasks/phase14-seed-rng-contract-gate-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Hotfix：run_flow contract rg fallback）

- 修复 `dev/tools/run_flow_contract_check.sh` 对 `rg` 的强依赖：
  - 有 `rg` 时使用 `rg` 搜索。
  - 无 `rg` 时自动降级为 `grep` 搜索。
- 目的：避免工程师在不同 shell/环境下因缺少 `rg` 反复误报 `workflow-check` 失败。

## 2026-02-17（Phase 15：save load replay smoke v1）

- 新增 `dev/tools/save_load_replay_smoke.sh` 冒烟验证脚本：
  - fixed-seed bootstrap check：验证 RunRng/RunLifecycleService 支持固定种子新局初始化
  - save/load rng continuity check：验证 RunRng 状态导出/恢复与 SaveService 存档/读档集成
  - battle->reward->map route smoke check：验证路由常量定义与核心流程方法存在
  - deterministic shuffle smoke check：验证 CardPile/PlayerHandler 确定性洗牌实现
- 不默认接入 workflow-check，原因：
  - 与 seed_rng_contract_check.sh、persistence_contract_check.sh 有部分重叠
  - 冒烟验证更适合在 verification 阶段或发布前手动执行
- 同步更新 `modules/seed_replay/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture`）。
- 新增任务三件套：`docs/tasks/phase15-save-load-replay-smoke-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 16：scene runstate write gate v1）

- 新增 `dev/tools/scene_runstate_write_check.sh` 场景层 RunState 写入门禁：
  - 禁止 `runtime/scenes/**/*.gd` 直接赋值 `run_state.<field> =`
  - 禁止复合赋值 `run_state.<field> +=/-=/*=/etc`
  - 禁止集合操作 `run_state.<field>.append/erase/clear/push_/pop_`
  - 禁止方法调用 `run_state.set_/add_/remove_/clear_/advance_/mark_/apply_`
  - 禁止嵌套写入 `run_state.player_stats.<field> =`
  - 允许只读访问（如 `run_state.gold` 读取）
- 接入 `dev/tools/workflow_check.sh` 作为默认质量门禁
- 同步更新架构文档（`module_boundaries_v1` / `module_architecture`）
- 新增任务三件套：`docs/tasks/phase16-scene-runstate-write-gate-v1/{plan,handoff,verification}.md`

## 2026-02-17（Phase 17：scene nested state write gate v1）

- 新增 `dev/tools/scene_nested_state_write_check.sh` 场景层嵌套状态写入门禁：
  - 禁止 `run_state.player_stats.(set_|add_|remove_|clear_|apply_|heal|take_damage|gain_block|set_status)` 方法调用
  - 禁止 `run_state.map_graph.(set_|add_|remove_|clear_|advance_)` 方法调用
  - 禁止 `run_state.(relics|potions|deck|discard|exhausted|consumables)` 集合操作
  - 禁止 `run_state.player_stats.(deck|discard|draw_pile|exhausted|consumables)` 集合操作
  - 目的：防止通过嵌套状态方法调用绕过 Phase 16 门禁
- 接入 `dev/tools/workflow_check.sh` 作为默认质量门禁
- 同步更新架构文档（`module_boundaries_v1` / `module_architecture`）
- 新增任务三件套：`docs/tasks/phase17-scene-nested-state-write-gate-v1/{plan,handoff,verification}.md`

## 2026-02-17（Phase 18：runflow payload contract gate v1）

- 新增 `dev/tools/run_flow_payload_contract_check.sh` run_flow payload 契约门禁：
  - 校验 `make_result` 函数签名（`next_route: String, payload: Dictionary = {}` -> Dictionary）
  - 校验 `make_result` 返回包含 `next_route` 字段
  - 校验 `map_flow.enter_map_node` 成功时返回包含 `accepted/node_id/node_type/reward_gold`
  - 校验 `map_flow.resolve_non_battle_completion` 返回包含 `node_type/bonus_log`
  - 校验 `battle_flow.resolve_battle_completion` 胜利时返回包含 `reward_gold`
  - 校验 `battle_flow.resolve_battle_completion` 失败时返回包含 `game_over_text`
  - 校验 `battle_flow.apply_battle_reward` 返回包含 `reward_log`
  - 校验所有返回必须通过 `route_dispatcher.make_result` 构造
  - 目的：防止路由返回结构被悄悄改坏
- 接入 `dev/tools/workflow_check.sh` 作为默认质量门禁
- 同步更新架构文档（`module_boundaries_v1` / `module_architecture`）
- 新增任务三件套：`docs/tasks/phase18-runflow-payload-contract-gate-v1/{plan,handoff,verification}.md`

## 2026-02-17（Phase 19：runflow result shape gate v1）

- 新增 `dev/tools/run_flow_result_shape_check.sh` run_flow 结果结构统一门禁：
  - 校验 `route_dispatcher.make_result` 函数存在且签名正确
  - 校验 `make_result` 返回包含 `next_route` 字段
  - 校验 `map_flow` 所有返回必须通过 `route_dispatcher.make_result` 构造
  - 校验 `battle_flow` 所有返回必须通过 `_result` 构造（最终调用 `make_result`）
  - 禁止 `map_flow/battle_flow` 直接返回手写字典（`return { ... }`）
  - 禁止 `map_flow/battle_flow` 直接返回包含 `next_route` 的字典
  - 目的：强制返回字典通过统一 helper 构造，减少键漂移
- 接入 `dev/tools/workflow_check.sh` 作为默认质量门禁
- 同步更新架构文档（`module_boundaries_v1` / `module_architecture`）
- 新增任务三件套：`docs/tasks/phase19-runflow-result-shape-gate-v1/{plan,handoff,verification}.md`
