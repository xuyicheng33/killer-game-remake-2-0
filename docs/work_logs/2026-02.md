# 2026-02 工作日志

## 2026-02-15

- 初始化协作脚手架：补齐 `docs/module_architecture.md`、模板文档、契约目录。
- 新增 `Makefile` 与 `tools` 脚本，支持 `make content-index`、`make workflow-check`、`make install-hooks`、`make new-task`。
- 校验并接入教程基线目录：`references/tutorial_baseline`。
- 迁移参考资料到 `references/slay_the_spire_cn`，并更新索引脚本路径。
- 新增模块缺口分析与 V2 开发路线图，包含资源全量替换任务拆解。

## 2026-02-16

- 完成 Phase 1「架构收口」文档交付：新增 `docs/contracts/module_boundaries_v1.md`。
- 回填 `docs/module_architecture.md`，按真实代码依赖重写模块总览与依赖方向。
- 回填 `docs/repo_structure.md`，明确“当前结构 + 目标结构 + 迁移原则”。
- 新增任务三件套：`docs/tasks/phase1-architecture-closure-v1/{plan,handoff,verification}.md`。
- 更新模块说明文档：`modules/README.md`、`modules/run_meta/README.md`、`modules/run_flow/README.md`、`modules/save_seed_replay/README.md`、`modules/ui_shell/README.md`。
- 明确命名收口建议：`persistence` 为存档主模块，`save_seed_replay` 冻结为占位，`run_flow` 作为后续应用编排归属目录。

## 2026-02-16（Phase 2 第一批）

- 新增 `run_flow` 应用服务骨架：`run_flow_service.gd` + `shop/event/rest` 命令服务。
- 改造 `scenes/shop/shop_screen.gd`：购买/删卡/离开不再直接写 `RunState`，改由 `ShopFlowService` 执行。
- 改造 `scenes/events/event_screen.gd`：事件选项应用与继续推进改由 `EventFlowService` 执行。
- 改造 `scenes/map/rest_screen.gd`：休息/强化写状态改由 `RestFlowService` 执行。
- 在 `scenes/app/app.gd` 注入 `RunFlowService` 并向页面下发子服务。
- 更新 `docs/contracts/module_boundaries_v1.md`、`docs/module_architecture.md`、`docs/repo_structure.md` 与当前实现对齐。
- 新增任务三件套：`docs/tasks/phase2-scene-decoupling-v1/{plan,handoff,verification}.md`。
- 修复联调崩溃：`scenes/ui/stats_ui.gd` 将 `Label.theme_override_font_sizes` 非法访问改为 `add_theme_font_size_override(\"font_size\", 18)`。

## 2026-02-16（Phase 3：battle flow decoupling v1）

- 新增 `modules/run_flow/battle_flow_service.gd`，将 battle 完成后的路由判定与奖励应用收口到应用服务层。
- 更新 `modules/run_flow/run_flow_service.gd`，接入 `battle_flow_service` 聚合注入。
- 改造 `scenes/app/app.gd`：迁移 battle 结束分支（胜利/失败）与奖励应用到 run_flow 命令调用，app 保留接线与页面切换。
- 更新 `modules/run_flow/README.md`，补充 battle -> reward -> map 命令返回契约。
- 同步更新 `docs/contracts/module_boundaries_v1.md`、`docs/module_architecture.md`、`docs/repo_structure.md`。
- 新增任务三件套：`docs/tasks/phase3-battle-flow-decoupling-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 4：map orchestration decoupling v1）

- 新增 `modules/run_flow/route_dispatcher.gd`，统一 `next_route + payload` 返回契约与路由常量。
- 新增 `modules/run_flow/map_flow_service.gd`，收口地图节点进入判定、placeholder 节点 `next_floor` 跳转、非战斗节点完成后路由决策。
- 更新 `modules/run_flow/run_flow_service.gd`，聚合注入 `route_dispatcher` 与 `map_flow_service`。
- 更新 `modules/run_flow/battle_flow_service.gd`，改为复用统一路由结果构造器。
- 瘦身 `scenes/app/app.gd`：移除 `enter_map_node` / placeholder / B3 bonus 编排写入，改为命令结果驱动页面实例化与信号接线。
- 更新 `modules/run_flow/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture` / `repo_structure`）。
- 新增任务三件套：`docs/tasks/phase4-map-orchestration-decoupling-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 5：flow context and contract tests v1）

- 新增 `modules/run_flow/flow_context.gd`，承接跨页面流程上下文（`pending_node_type` / `pending_reward_gold`）。
- 更新 `modules/run_flow/run_flow_service.gd`，聚合 `flow_context` 并向 app 暴露上下文读写入口。
- 瘦身 `scenes/app/app.gd`：删除本地 `pending_*` 字段，改为通过 `run_flow_service` 获取流程上下文。
- 更新 `modules/run_flow/battle_flow_service.gd`：删除重复路由常量，统一复用 `RunRouteDispatcher` 常量单点定义。
- 新增最小契约检查脚本 `tools/run_flow_contract_check.sh`，覆盖 map_flow/battle_flow 关键路由与 payload 契约。
- 同步更新 `modules/run_flow/README.md`、`docs/contracts/module_boundaries_v1.md`、`docs/module_architecture.md`、`docs/repo_structure.md`。
- 新增任务三件套：`docs/tasks/phase5-flow-context-and-contract-tests-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 6：ui shell viewmodel decoupling v1）

- 在 `modules/ui_shell` 新增首批壳层实现：
  - `viewmodel/stats_view_model.gd`
  - `viewmodel/relic_potion_view_model.gd`
  - `adapter/stats_ui_adapter.gd`
  - `adapter/relic_potion_ui_adapter.gd`
- 改造 `scenes/ui/stats_ui.gd`：改为读取 `StatsUIAdapter` 投影并渲染，不再直接拼装状态徽章数据。
- 改造 `scenes/ui/relic_potion_ui.gd`：改为读取 `RelicPotionUIAdapter` 投影并通过 adapter 转发“使用药水”命令。
- 同步更新 `modules/ui_shell/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture` / `repo_structure`）。
- 新增任务三件套：`docs/tasks/phase6-ui-shell-viewmodel-decoupling-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 7：quality gates and regression v1）

- 新增 `tools/ui_shell_contract_check.sh`：
  - 拦截 `scenes/ui` 直接调用 `run_state.set_/add_/remove_/clear_/advance_/mark_/apply_`。
  - 校验 `stats_ui`、`relic_potion_ui` 仍通过 adapter/viewmodel 接入。
- 扩展 `tools/run_flow_contract_check.sh`：
  - 新增 `ROUTE_*` 常量单点定义校验（仅允许 `route_dispatcher.gd` 定义）。
  - 扩展 map/battle 关键 `next_route + payload` 键位回归检查。
- 更新 `tools/workflow_check.sh`：
  - 在白名单检查后串行执行 `ui_shell_contract_check.sh` 与 `run_flow_contract_check.sh`，形成提交前必过门禁。
- 同步更新 `modules/ui_shell/README.md`、`modules/run_flow/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture` / `repo_structure`）。
- 新增任务三件套：`docs/tasks/phase7-quality-gates-and-regression-v1/{plan,handoff,verification}.md`。

## 2026-02-17（chore-ai-planner-reviewer-character-prompts-v1）

- 新增协作手册：`docs/ai_planner_reviewer_playbook.md`，固化 Planner + Reviewer 工作流与审核通过门槛。
- 新增工程提示词模板：`docs/prompts/engineering_rpi_prompts.md`，覆盖 Research/Plan/Implement/Self Review/Handoff 五阶段。
- 新增人物提示词套件：`docs/prompts/character_portrait_prompts/20_角色提示词套件_v2.md`，包含霜北刀/离恨烟/埋骨钱的可直接投喂文本、批量生成模板与二次修正模板。
- 更新提示词索引：`docs/prompts/README.md`、`docs/prompts/character_portrait_prompts/README.md`。
- 新增任务三件套：`docs/tasks/chore-ai-planner-reviewer-character-prompts-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 9：seed deterministic draw shuffle v1）

- 在 `content/custom_resources/card_pile.gd` 新增 `shuffle_with_rng(stream_key)`，使用 `RunRng` + Fisher-Yates 实现确定性洗牌。
- 在 `runtime/scenes/player/player_handler.gd` 替换战斗链路洗牌调用：
  - 战斗开始抽牌堆洗牌：`battle_start_shuffle`
  - 弃牌回洗抽牌堆：`reshuffle_discard`
- 更新 `runtime/modules/seed_replay/README.md`，记录确定性洗牌落点与 stream key 约束。
- 新增任务三件套：`docs/tasks/phase9-seed-deterministic-draw-shuffle-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 8：ui shell battle_ui decoupling v1）

- 在 `modules/ui_shell` 新增 `battle_ui` 壳层实现：
  - `viewmodel/battle_ui_view_model.gd`
  - `adapter/battle_ui_adapter.gd`
- 改造 `scenes/ui/battle_ui.gd`：移除对 `CardZonesModel` 的直接依赖，改为通过 `BattleUIAdapter` 获取牌区计数投影并转发"结束回合"命令。
- 扩展 `tools/ui_shell_contract_check.sh`：
  - 新增 `battle_ui` 接线校验（必须通过 adapter/viewmodel）。
  - 新增禁止 `battle_ui.gd` 直接导入 `card_system/card_zones_model` 的检查。
- 同步更新 `modules/ui_shell/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture`）。
- 新增任务三件套：`docs/tasks/phase8-ui-shell-battle-ui-decoupling-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 10：persistence status serialization v1）

- 在 `runtime/modules/persistence/save_service.gd` 新增玩家状态层序列化与恢复：
  - 升级 `SAVE_VERSION` 为 v2，新增 `MIN_COMPAT_VERSION` 为 v1。
  - 在 `_serialize_player_stats` 中新增 `statuses` 字段，调用 `stats.get_status_snapshot()` 获取状态快照。
  - 在 `_apply_player_stats` 中新增状态层恢复逻辑，遍历 `statuses` 字典调用 `stats.set_status()`。
- 实现存档版本向后兼容：
  - v1 存档读取时，`statuses` 字段缺失则使用空字典默认值，不恢复任何状态层。
  - v2 存档读取时，按 `statuses` 字典恢复各状态层数值。
- 同步更新 `docs/contracts/run_state.md`：
  - 新增 `player_stats.statuses` 字段说明。
  - 新增版本兼容策略说明。
  - 新增 `statuses = {}` 默认值说明。
- 同步更新 `runtime/modules/persistence/README.md`：
  - 新增 Phase 10 状态与职责说明。
  - 新增存档版本与兼容策略说明。
- 新增任务三件套：`docs/tasks/phase10-persistence-status-serialization-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 11：run flow app lifecycle decoupling v1）

- 新增 `modules/run_flow/run_lifecycle_service.gd`，收口生命周期逻辑：
  - `start_new_run()`：新局初始化（含 RNG/REPRO 初始化）。
  - `try_load_saved_run()`：读档恢复（含 RNG 状态恢复）。
  - `save_checkpoint()`：checkpoint 存档。
  - `update_repro_progress()` / `log_node_enter()`：复盘日志更新。
- 更新 `modules/run_flow/run_flow_service.gd`，聚合注入 `lifecycle_service`。
- 瘦身 `scenes/app/app.gd`：
  - 移除对 `persistence`、`run_rng`、`repro_log` 的直接依赖。
  - 新局初始化、读档恢复、checkpoint 存档、复盘日志更新改为通过 `run_flow_service.lifecycle_service` 调用。
  - `app.gd` 保持"页面实例化 + 路由分发 + 事件接线"薄层职责。
- 同步更新 `modules/run_flow/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture`）。
- 新增任务三件套：`docs/tasks/phase11-run-flow-app-lifecycle-decoupling-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 12：run flow lifecycle contract gate v1）

- 新增 `dev/tools/run_lifecycle_contract_check.sh`：
  - 禁止 `scenes/app/app.gd` 直接 preload/use `persistence/save_service.gd`、`run_rng.gd`、`repro_log.gd`。
  - 强制 `app.gd` 通过 `run_flow_service.lifecycle_service` 调用 `start_new_run/try_load_saved_run/save_checkpoint`。
  - 目的：防止后续回归把生命周期逻辑再次耦合到入口场景。
- 更新 `dev/tools/workflow_check.sh`，串行纳入 `run_lifecycle_contract_check.sh`。
- 同步更新 `modules/run_flow/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture`）。
- 新增任务三件套：`docs/tasks/phase12-run-flow-lifecycle-contract-gate-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 13：persistence contract gate v1）

- 新增 `dev/tools/persistence_contract_check.sh`：
  - 校验 `SAVE_VERSION` 与 `MIN_COMPAT_VERSION` 常量存在。
  - 校验 `_serialize_player_stats` 包含 `statuses` 字段（来自 `get_status_snapshot`）。
  - 校验 `_apply_player_stats` 包含 `statuses` 恢复逻辑（调用 `set_status`）。
  - 校验读取 `statuses` 时对旧存档有默认空字典兜底（兼容 v1）。
  - 目的：防止后续改动破坏 phase10 的"状态层存档兼容"能力。
- 更新 `dev/tools/workflow_check.sh`，串行纳入 `persistence_contract_check.sh`。
- 同步更新 `modules/persistence/README.md`、`docs/contracts/run_state.md` 与架构文档（`module_boundaries_v1` / `module_architecture`）。
- 新增任务三件套：`docs/tasks/phase13-persistence-contract-gate-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 14：seed rng contract gate v1）

- 新增 `dev/tools/seed_rng_contract_check.sh`：
  - 校验 `card_pile.gd` 存在 `shuffle_with_rng(stream_key)` 方法。
  - 校验 `shuffle_with_rng` 内使用 `RunRng.randi_range`（非系统默认 shuffle）。
  - 校验 `player_handler.gd` 的 `start_battle` 使用 `shuffle_with_rng("battle_start_shuffle")`。
  - 校验 `player_handler.gd` 的 `reshuffle_deck_from_discard` 使用 `shuffle_with_rng("reshuffle_discard")`。
  - 校验 `run_lifecycle_service.gd` 存在 `restore_run_state` 逻辑。
  - 校验 `run_lifecycle_service.gd` 存在 `begin_run` 回退逻辑。
  - 目的：防止后续改动破坏"确定性洗牌 + 读档随机流连续性"约束。
- 更新 `dev/tools/workflow_check.sh`，串行纳入 `seed_rng_contract_check.sh`。
- 同步更新 `modules/seed_replay/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture`）。
- 新增任务三件套：`docs/tasks/phase14-seed-rng-contract-gate-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Hotfix：run_flow contract rg fallback）

- 修复 `dev/tools/run_flow_contract_check.sh` 对 `rg` 的强依赖：
  - 有 `rg` 时使用 `rg` 搜索。
  - 无 `rg` 时自动降级为 `grep` 搜索。
- 目的：避免工程师在不同 shell/环境下因缺少 `rg` 反复误报 `workflow-check` 失败。

## 2026-02-17（Phase 15：save load replay smoke v1）

- 新增 `dev/tools/save_load_replay_smoke.sh` 冒烟验证脚本：
  - fixed-seed bootstrap check：验证 RunRng/RunLifecycleService 支持固定种子新局初始化
  - save/load rng continuity check：验证 RunRng 状态导出/恢复与 SaveService 存档/读档集成
  - battle->reward->map route smoke check：验证路由常量定义与核心流程方法存在
  - deterministic shuffle smoke check：验证 CardPile/PlayerHandler 确定性洗牌实现
- 不默认接入 workflow-check，原因：
  - 与 seed_rng_contract_check.sh、persistence_contract_check.sh 有部分重叠
  - 冒烟验证更适合在 verification 阶段或发布前手动执行
- 同步更新 `modules/seed_replay/README.md` 与架构文档（`module_boundaries_v1` / `module_architecture`）。
- 新增任务三件套：`docs/tasks/phase15-save-load-replay-smoke-v1/{plan,handoff,verification}.md`。

## 2026-02-17（Phase 16：scene runstate write gate v1）

- 新增 `dev/tools/scene_runstate_write_check.sh` 场景层 RunState 写入门禁：
  - 禁止 `runtime/scenes/**/*.gd` 直接赋值 `run_state.<field> =`
  - 禁止复合赋值 `run_state.<field> +=/-=/*=/etc`
  - 禁止集合操作 `run_state.<field>.append/erase/clear/push_/pop_`
  - 禁止方法调用 `run_state.set_/add_/remove_/clear_/advance_/mark_/apply_`
  - 禁止嵌套写入 `run_state.player_stats.<field> =`
  - 允许只读访问（如 `run_state.gold` 读取）
- 接入 `dev/tools/workflow_check.sh` 作为默认质量门禁
- 同步更新架构文档（`module_boundaries_v1` / `module_architecture`）
- 新增任务三件套：`docs/tasks/phase16-scene-runstate-write-gate-v1/{plan,handoff,verification}.md`

## 2026-02-17（Phase 17：scene nested state write gate v1）

- 新增 `dev/tools/scene_nested_state_write_check.sh` 场景层嵌套状态写入门禁：
  - 禁止 `run_state.player_stats.(set_|add_|remove_|clear_|apply_|heal|take_damage|gain_block|set_status)` 方法调用
  - 禁止 `run_state.map_graph.(set_|add_|remove_|clear_|advance_)` 方法调用
  - 禁止 `run_state.(relics|potions|deck|discard|exhausted|consumables)` 集合操作
  - 禁止 `run_state.player_stats.(deck|discard|draw_pile|exhausted|consumables)` 集合操作
  - 目的：防止通过嵌套状态方法调用绕过 Phase 16 门禁
- 接入 `dev/tools/workflow_check.sh` 作为默认质量门禁
- 同步更新架构文档（`module_boundaries_v1` / `module_architecture`）
- 新增任务三件套：`docs/tasks/phase17-scene-nested-state-write-gate-v1/{plan,handoff,verification}.md`

## 2026-02-17（Phase 18：runflow payload contract gate v1）

- 新增 `dev/tools/run_flow_payload_contract_check.sh` run_flow payload 契约门禁：
  - 校验 `make_result` 函数签名（`next_route: String, payload: Dictionary = {}` -> Dictionary）
  - 校验 `make_result` 返回包含 `next_route` 字段
  - 校验 `map_flow.enter_map_node` 成功时返回包含 `accepted/node_id/node_type/reward_gold`
  - 校验 `map_flow.resolve_non_battle_completion` 返回包含 `node_type/bonus_log`
  - 校验 `battle_flow.resolve_battle_completion` 胜利时返回包含 `reward_gold`
  - 校验 `battle_flow.resolve_battle_completion` 失败时返回包含 `game_over_text`
  - 校验 `battle_flow.apply_battle_reward` 返回包含 `reward_log`
  - 校验所有返回必须通过 `route_dispatcher.make_result` 构造
  - 目的：防止路由返回结构被悄悄改坏
- 接入 `dev/tools/workflow_check.sh` 作为默认质量门禁
- 同步更新架构文档（`module_boundaries_v1` / `module_architecture`）
- 新增任务三件套：`docs/tasks/phase18-runflow-payload-contract-gate-v1/{plan,handoff,verification}.md`

## 2026-02-17（Phase 19：runflow result shape gate v1）

- 新增 `dev/tools/run_flow_result_shape_check.sh` run_flow 结果结构统一门禁：
  - 校验 `route_dispatcher.make_result` 函数存在且签名正确
  - 校验 `make_result` 返回包含 `next_route` 字段
  - 校验 `map_flow` 所有返回必须通过 `route_dispatcher.make_result` 构造
  - 校验 `battle_flow` 所有返回必须通过 `_result` 构造（最终调用 `make_result`）
  - 禁止 `map_flow/battle_flow` 直接返回手写字典（`return { ... }`）
  - 禁止 `map_flow/battle_flow` 直接返回包含 `next_route` 的字典
  - 目的：强制返回字典通过统一 helper 构造，减少键漂移
- 接入 `dev/tools/workflow_check.sh` 作为默认质量门禁
- 同步更新架构文档（`module_boundaries_v1` / `module_architecture`）
- 新增任务三件套：`docs/tasks/phase19-runflow-result-shape-gate-v1/{plan,handoff,verification}.md`

## 2026-02-17（Phase 20：workflow untracked whitelist gate v1）

- 修复 `dev/tools/workflow_check.sh` 白名单检查漏洞：
  - 原有问题：只检查 `git diff --name-only --cached` 和 `git diff --name-only`，未覆盖 untracked 文件
  - 新增检查：`git ls-files --others --exclude-standard` 纳入 untracked 文件
  - 目的：确保白名单外新建文件也会阻断提交
- 新增任务三件套：`docs/tasks/phase20-workflow-untracked-whitelist-gate-v1/{plan,handoff,verification}.md`

## 2026-02-17（Phase 21：workflow branch taskid gate v1）

- 新增 `dev/tools/workflow_check.sh` 分支名与 TASK_ID 一致性门禁：
  - 原有问题：分支名格式校验通过，但未检查分支名是否包含 TASK_ID
  - 新增检查：非 main 分支必须包含当前 TASK_ID
  - 目的：避免在错误分支上提交错误任务产物
- 新增任务三件套：`docs/tasks/phase21-workflow-branch-taskid-gate-v1/{plan,handoff,verification}.md`

## 2026-02-17（Phase 22：workflow branch gate selfcheck v1）

- 新增 `dev/tools/workflow_branch_gate_selfcheck.sh` 分支门禁自检脚本：
  - 场景 1：分支名格式非法 -> 失败
  - 场景 2：分支名合法但不含 TASK_ID -> 失败
  - 场景 3：分支名合法且包含 TASK_ID -> 通过
  - 目的：自动验证分支门禁逻辑的正确性
- 新增任务三件套：`docs/tasks/phase22-workflow-branch-gate-selfcheck-v1/{plan,handoff,verification}.md`

## 2026-02-17（R2：toolchain-first master planning + reviewer prompts v1）

- 新增 `docs/roadmap/r2_toolchain_first_master_plan_v1.md`：
  - 从 `r2-phase00` 起重建全新阶段链路，和历史 `phase*` 任务编号彻底区分。
  - 固化"工具链闭环优先、内容扩容其次、视觉资源替换最后"的推进顺序。
  - 明确每阶段目标、等级、主模块、验收命令、风险与退出条件。
- 新增 `docs/prompts/r2_task_publisher_reviewer_prompts_v1.md`：
  - 提供"任务发布者 + 审核员"主控提示词（上下文恢复 -> 派发 -> 审核 -> 提交 -> 推进下一任务）。
  - 提供编程员任务卡模板、审核模板、固定输出模板、自动推进下一阶段模板。
- 回填索引：
  - `docs/roadmap/README.md` 增加 R2 主规划入口。
  - `docs/prompts/README.md` 增加 R2 审核发布提示词入口。

## 2026-02-17（R2 Phase 0 基线快照）

### 基线 Commit

- Commit Hash: `2b1ab22`
- Commit Message: `docs(roadmap): add R2 toolchain-first plan and reviewer prompt kit（r2-phase00-baseline-snapshot-v1）`

### 已完成架构收口摘要（Phase 1-22）

| Phase | 主模块 | 核心成果 |
|---|---|---|
| 1 | `run_meta` | 模块边界契约 `module_boundaries_v1.md`，依赖方向固化 |
| 2 | `run_flow` | Shop/Event/Rest 场景解耦，命令服务收口 |
| 3 | `run_flow` | Battle 完成路由判定与奖励应用收口 |
| 4 | `run_flow` | RouteDispatcher 统一返回契约，MapFlowService 地图编排 |
| 5 | `run_flow` | FlowContext 跨页面流程上下文，契约检查脚本 |
| 6 | `ui_shell` | Stats/RelicPotion ViewModel + Adapter 壳层 |
| 7 | `run_meta` | UI/RunFlow 契约门禁，workflow-check 闭环 |
| 8 | `ui_shell` | BattleUI ViewModel + Adapter 壳层 |
| 9 | `seed_replay` | CardPile 确定性洗牌（Fisher-Yates + RunRng） |
| 10 | `persistence` | 玩家状态层序列化与恢复，存档版本向后兼容 |
| 11 | `run_flow` | RunLifecycleService 生命周期收口 |
| 12 | `run_flow` | 生命周期契约门禁 |
| 13 | `persistence` | 存档契约门禁 |
| 14 | `seed_replay` | 种子 RNG 契约门禁 |
| 15 | `seed_replay` | 存档/读档/复盘冒烟脚本 |
| 16 | `run_meta` | 场景层 RunState 写入门禁 |
| 17 | `run_meta` | 场景层嵌套状态写入门禁 |
| 18 | `run_flow` | Payload 契约门禁 |
| 19 | `run_flow` | 结果结构统一门禁 |
| 20 | `run_meta` | Untracked 文件白名单门禁 |
| 21 | `run_meta` | 分支名与 TASK_ID 一致性门禁 |
| 22 | `run_meta` | 分支门禁自检脚本 |

### R2 任务总表

| R2 Phase | 任务 ID | 等级 | 主模块 | 目标摘要 | 状态 | 依赖 | 负责人 |
|---|---|---|---|---|---|---|---|
| 0 | `r2-phase00-baseline-snapshot-v1` | L0 | `run_meta` | 固化 R2 基线、状态面板、执行清单 | done | - | - |
| 1 | `r2-phase01-workflow-gate-hardening-v1` | L1 | `run_meta` | 补齐 workflow-check 自检与跨环境稳定性 | planned | Phase 0 | - |
| 2 | `r2-phase02-audit-pipeline-bootstrap-v1` | L1 | `run_meta` | 建立"发布-实现-审核-提交"标准闭环脚手架 | planned | Phase 1 | - |
| 3 | `r2-phase03-ui-shell-full-decoupling-v1` | L2 | `ui_shell` | 完成 map/rest/shop/event/reward UI 壳层迁移 | planned | Phase 2 | - |
| 4 | `r2-phase04-run-flow-regression-gate-v1` | L1 | `run_flow` | 扩展 run_flow 分支契约回归门禁 | planned | Phase 3 | - |
| 5 | `r2-phase05-save-load-replay-runtime-smoke-v1` | L1 | `seed_replay` | 增强运行时冒烟，补齐主链路可复现检查 | planned | Phase 4 | - |
| 6 | `r2-phase06-content-schema-expansion-v1` | L2 | `content_pipeline` | 设计 enemy/relic/event schema 与校验规则 | planned | Phase 5 | - |
| 7 | `r2-phase07-content-importers-expansion-v1` | L2 | `content_pipeline` | 实装 enemy/relic/event 导入脚本与报告 | planned | Phase 6 | - |
| 8 | `r2-phase08-content-pipeline-gate-integration-v1` | L1 | `content_pipeline` | 接入 workflow 或发布前强制门禁 | planned | Phase 7 | - |
| 9 | `r2-phase09-character2-scaffold-v1` | L2 | `run_meta` | 第二角色骨架与开局接线 | planned | Phase 8 | - |
| 10 | `r2-phase10-enemy-pack-v1` | L2 | `enemy_intent` | 数据驱动遭遇选择 + 普通/精英敌扩容 | planned | Phase 9 | - |
| 11 | `r2-phase11-relic-potion-event-pack-v1` | L2 | `relic_potion` | 扩展遗物/药水/事件内容池与联动 | planned | Phase 10 | - |
| 12 | `r2-phase12-art-asset-replacement-v2` | L1 | `ui_shell` | 最终资源替换轨道（最后执行） | planned | Phase 11 | - |

## 2026-02-17（R2 Phase 1：workflow-gate-hardening）

### 改动摘要

- 新增 `dev/tools/workflow_gate_selfcheck.sh` 门禁自检脚本：
  - 检查 rg/grep 搜索后端降级逻辑
  - 验证分支名格式校验逻辑
  - 验证 TASK_ID 对齐逻辑
  - 验证白名单阻断逻辑
- 确认现有脚本 rg 依赖状态：
  - `run_flow_contract_check.sh` 已有 rg 检测与 grep 降级逻辑
  - 其他脚本（ui_shell_contract_check.sh、persistence_contract_check.sh、seed_rng_contract_check.sh 等）仅使用 grep，无 rg 依赖
- 新增任务三件套：`docs/tasks/r2-phase01-workflow-gate-hardening-v1/{plan,handoff,verification}.md`

### 验证结果

- [x] `bash dev/tools/workflow_gate_selfcheck.sh` 通过
- [x] 分支名与 TASK_ID 一致性门禁验证正确

## 2026-02-17（R2 Phase 2：audit-pipeline-bootstrap）

### 改动摘要

- 新增 `docs/templates/auditor_output_template.md` 审核员输出模板：
  - 固化审核结论格式（通过/不通过）
  - Findings 分级（Critical > High > Medium > Low）
  - 验证结果表格
  - 提交信息格式
  - 风险与未覆盖验证点
  - 下一任务提示词生成模板
- 新增 `docs/templates/programmer_task_template.md` 程序员任务卡模板：
  - 任务元信息（ID/等级/主模块）
  - 目标与边界定义
  - 必做项清单
  - 白名单文件列表
  - 任务三件套说明
  - 验证命令格式
  - 禁止项 checklist
  - 交付要求与 commit message 格式
- 更新 `docs/prompts/r2_task_publisher_reviewer_prompts_v1.md`：
  - 增强"自动推进下一阶段"规则
  - 添加查找下一任务、自动填充字段、禁止项附加等详细规则
  - 引用新创建的模板文件
  - 添加自动推进流程图与示例输出格式
- 新增任务三件套：`docs/tasks/r2-phase02-audit-pipeline-bootstrap-v1/{plan,handoff,verification}.md`

### 验证结果

- [x] `ls docs/templates/` 确认模板文件存在
- [x] `make workflow-check TASK_ID=r2-phase02-audit-pipeline-bootstrap-v1` 通过

## 2026-02-17（R2 Phase 3：ui-shell-full-decoupling）

### 改动摘要

- 在 `modules/ui_shell` 新增 5 个 viewmodel：
  - `map_ui_view_model.gd`：将 `RunState` / `MapGraphData` 投影为地图节点展示数据。
  - `rest_ui_view_model.gd`：将 `RunState` 投影为休息界面展示数据。
  - `shop_ui_view_model.gd`：将 `RunState` / 商店商品投影为商店界面展示数据。
  - `event_ui_view_model.gd`：将事件模板投影为事件界面展示数据。
  - `reward_ui_view_model.gd`：将 `RewardBundle` 投影为奖励界面展示数据。
- 在 `modules/ui_shell` 新增 5 个 adapter：
  - `map_ui_adapter.gd`：监听 `RunState.changed`，向 `map_screen` 推送地图投影并转发节点选择/重启命令。
  - `rest_ui_adapter.gd`：监听 `RunState.changed`，向 `rest_screen` 推送休息界面投影并转发休息/升级命令。
  - `shop_ui_adapter.gd`：监听 `RunState.changed`，向 `shop_screen` 推送商店投影并转发购买/移除/离开命令。
  - `event_ui_adapter.gd`：向 `event_screen` 推送事件投影并转发选项选择/继续命令。
  - `reward_ui_adapter.gd`：向 `reward_screen` 推送奖励投影并转发卡牌选择/跳过命令。
- 改造 5 个场景脚本：
  - `map_screen.gd`：改为读取 `MapUIAdapter` 投影并发送节点选择/重启命令。
  - `rest_screen.gd`：改为读取 `RestUIAdapter` 投影并发送休息/升级命令。
  - `shop_screen.gd`：改为读取 `ShopUIAdapter` 投影并发送购买/移除/离开命令。
  - `event_screen.gd`：改为读取 `EventUIAdapter` 投影并发送选项选择/继续命令。
  - `reward_screen.gd`：改为读取 `RewardUIAdapter` 投影并发送卡牌选择/跳过命令。
- 同步更新 `modules/ui_shell/README.md`，记录完整 UI 壳层架构。
- 新增任务三件套：`docs/tasks/r2-phase03-ui-shell-full-decoupling-v1/{plan,handoff,verification}.md`

## 2026-02-17（R2 Phase 4：run-flow-regression-gate-v1）

### 改动摘要

- 新增 `dev/tools/run_flow_regression_check.sh` 回归门禁脚本：
  - 覆盖 rest_flow_service、shop_flow_service、event_flow_service 三条非战斗分支
  - 检查 _result 封装函数存在性
  - 检查是否调用 route_dispatcher.make_result
  - 检查返回键位是否符合契约（completed/info_text、handled/status_text）
  - 检测直接返回手写字典的情况
  - 使用 WARN 级别暴露契约偏差，不阻塞流程（为后续改造预留空间）
- 更新 `dev/tools/workflow_check.sh`，串行纳入 `run_flow_regression_check.sh`
- 新增任务三件套：`docs/tasks/r2-phase04-run-flow-regression-gate-v1/{plan,handoff,verification}.md`

### 当前发现的契约偏差

| 服务 | 问题 | 严重程度 |
|------|------|----------|
| rest_flow_service | _result 未调用 make_result | 中等 |
| rest_flow_service | 直接返回手写字典 | 中等 |
| shop_flow_service | _result 未调用 make_result | 中等 |
| shop_flow_service | 直接返回手写字典 | 中等 |
| event_flow_service | 缺少 _result 封装 | 高 |
| event_flow_service | execute_option 返回 String | 高 |

### 验证结果

- [x] `bash dev/tools/run_flow_regression_check.sh` 通过（WARN 级别检测正确）

## 2026-02-17（R2 Phase 5：save-load-replay-runtime-smoke-v1）

### 改动摘要

- 增强 `dev/tools/save_load_replay_smoke.sh` 运行时冒烟验证脚本：
  - **保留原检查（4组）**：
    - fixed-seed bootstrap check
    - save/load rng continuity check
    - battle->reward->map route smoke check
    - deterministic shuffle smoke check
  - **新增检查（5组）**：
    - exception path: restore failure fallback - 读档失败时使用 begin_run 回退
    - save version compatibility check - v1/v2 版本兼容性与默认值处理
    - environment seed override check - STS_RUN_SEED 环境变量覆盖
    - repro log continuity check - 复盘日志在新局/读档时的连续性
    - runtime main link integrity check - 文件操作、JSON 序列化、存档清理
- 明确文档说明：此脚本**不接入** workflow-check
  - 原因：与契约门禁有部分重叠，更适合 verification 阶段或发布前手动执行
- 更新 `runtime/modules/seed_replay/README.md` 记录增强后的冒烟验证
- 新增任务三件套：`docs/tasks/r2-phase05-save-load-replay-runtime-smoke-v1/{plan,handoff,verification}.md`

### 验证结果

- [x] `bash dev/tools/save_load_replay_smoke.sh` 通过（54 个断言全部通过）
- [x] 9 组检查全部通过
- [x] 脚本输出明确提示：不接入 workflow-check，发布前手动执行

## 2026-02-17（R2 Phase 6：content-schema-expansion-v1）

### 改动摘要（已审批 L2 任务）

- 新增 Schema 定义（3 个）：
  - `runtime/modules/content_pipeline/schemas/enemy_schema.json` - 敌人数据 schema
  - `runtime/modules/content_pipeline/schemas/relic_schema.json` - 遗物数据 schema
  - `runtime/modules/content_pipeline/schemas/event_schema.json` - 事件数据 schema
  - `runtime/modules/content_pipeline/schemas/README.md` - Schema 设计原则与版本管理
- 新增正例样例数据（3 组）：
  - `sources/enemies/examples/act1_enemies.json` - 3 个敌人 + 4 个遭遇组合
  - `sources/relics/examples/common_relics.json` - 5 个遗物
  - `sources/events/examples/baseline_events.json` - 5 个基础事件示例
- 新增反例样例数据（7 个）：
  - enemies/relics/events 各 2-3 个无效数据示例，用于验证错误检测
- 统一错误报告字段规范：
  - source/field/code/message 四字段模型
  - 错误代码约定：MISSING_REQUIRED, INVALID_TYPE, INVALID_VALUE 等
- 更新模块文档：
  - `runtime/modules/content_pipeline/README.md` - 更新内容类型状态表格
  - `docs/module_architecture.md` - 更新 content_pipeline 条目
- 新增任务三件套：`docs/tasks/r2-phase06-content-schema-expansion-v1/{plan,handoff,verification}.md`

### 为 Phase 7 奠定的契约基础

1. **Schema 版本策略**: MAJOR/MINOR/PATCH 规则已定义
2. **验证规则**: JSON Schema Draft-07 格式，可直接使用验证库
3. **错误代码**: 统一前缀约定（ENEMY_, RELIC_, EVENT_）
4. **正例/反例**: 提供测试数据，可用于导入器单元测试
5. **数据映射**: Schema 字段与现有 Godot 资源类对齐

### 验证结果

- [x] `python3 dev/tools/content_import_cards.py --input runtime/modules/content_pipeline/sources/cards --report /tmp/cards_check.json` - Cards 导入器回归通过
- [x] `make workflow-check TASK_ID=r2-phase06-content-schema-expansion-v1` - Workflow 门禁通过
- [x] 所有 JSON Schema 格式有效
- [x] 正例/反例样例数据完整

## 2026-02-18（R2 Phase 7-11 进度回填）

### 回填目的

- 修复“代码已推进但状态面板/日志停留在 Phase 6”的文档偏差。
- 将 R2 Phase 7-11 的完成状态与当前分支代码对齐，避免交接误判。

### Phase 7：content-importers-expansion

- 已落地 enemy/relic/event 导入器，输出统一报告到 `runtime/modules/content_pipeline/reports/`。
- 核心脚本：
  - `dev/tools/content_import_enemies.py`
  - `dev/tools/content_import_relics.py`
  - `dev/tools/content_import_events.py`

### Phase 8：content-pipeline-gate-integration

- `dev/tools/content_pipeline_check.sh` 已形成一键导入校验入口。
- 本次复验结果：
  - cards: ok
  - enemies: ok
  - relics: ok
  - events: ok

### Phase 9：character2-scaffold

- `runtime/modules/run_meta/character_registry.gd` 已支持 warrior/mage 双角色注册与环境变量选择。
- `run_state` 与 `save_service` 已支持 `character_id` 存取兼容链路。

### Phase 10：enemy-pack

- `encounter_registry.gd` 已接入基于楼层/标签/权重的数据驱动遭遇选择。
- `battle.tscn` 已移除 BatEnemy/CrabEnemy 硬编码入口，改为运行时动态生成。

### Phase 11：relic-potion-event-pack

- 奖励系统已使用 `relic_catalog/potion_catalog` 替代硬编码样例。
- 资源池规模：
  - relic `.tres`: 4
  - potion `.tres`: 3
- 事件模板扩容到 15 条（`event_catalog.gd`）。

## 2026-02-18（收尾修复：门禁与验证闭环）

### 改动摘要

- 修复 `save_load_replay_smoke.sh` 对 `start_new_run_with_seed` 的误报正则，恢复冒烟稳定性。
- 收口 rest/shop/event 返回契约：
  - 统一通过 `route_dispatcher.make_result` 构造返回。
  - `execute_option/execute_continue/execute_leave` 统一为 `Dictionary` 返回。
- 加固 `run_flow_regression_check.sh`：
  - 从 WARN 提示改为阻塞式 FAIL。
  - 强制非战斗分支返回签名和 payload 键位契约。
- 同步更新 `docs/r2_baseline_status.md`、Phase 9/10/11 verification/handoff，移除“待执行”占位状态。

### 验证结果

- [x] `bash dev/tools/run_flow_regression_check.sh` 通过
- [x] `bash dev/tools/save_load_replay_smoke.sh` 通过

## 2026-02-18（Phase 2 审核收口）

### 范围

- `feat-effect-stack-v2`
- `feat-buff-system-v2`
- `feat-battle-loop-state-machine-v2`
- `feat-relic-potion-v2`
- `feat-map-graph-v2`
- `feat-reward-economy-v2`

### 收口动作

- 完成 Phase 2 六个子任务文档复核（plan/handoff/verification）。
- 修复商店编译兼容问题（`ShopOfferGenerator.BUY_PRICE/REMOVE_PRICE` 常量兼容 + 调用链更新）。
- 统一删卡路径到 `ShopOfferGenerator.remove_card()`，避免价格规则分叉。

### 结果

- [x] `make test` 通过（58/58）
- [x] Phase 2 审核结论：通过
- [x] 已进入 Phase 3a / Phase 4 准备阶段

## 2026-02-19（UI/Tooltip 修复批次）

### 任务列表

- `fix-tooltip-event-signature-v1` - 统一 Tooltip 信号签名
- `feat-card-display-name-v1` - 卡牌显示名称字段与集成
- `feat-tooltip-extension-v1` - Tooltip 扩展到非战斗场景
- `content-cards-chinese-v1` - 卡牌中文化
- `fix-ui-issues-v1` - UI 布局响应式修复

### 改动摘要

- **fix-tooltip-event-signature-v1**：统一 `card_tooltip_requested` 信号签名为 `(icon: Texture, text: String)`。
- **feat-card-display-name-v1**：Card 类新增 `display_name` 字段，内容导入脚本映射 JSON `name` 到 `display_name`，Shop/Reward 显示中文名称，存档支持序列化/反序列化。
- **feat-tooltip-extension-v1**：新增 `relic_tooltip_requested`/`potion_tooltip_requested` 信号，Shop/Reward/RelicPotionUI 集成 Tooltip 功能。
- **content-cards-chinese-v1**：`warrior_cards.json` 所有 20 张卡牌翻译为中文，重新执行导入。
- **fix-ui-issues-v1**：Battle UI 区域计数响应式布局，休息/事件屏幕文本溢出处理。

### 验证结果

- [x] GUT 全量测试通过（126→131）

## 2026-02-20（审核修复批次）

### 任务列表

- `chore-perf-memory-baseline-v1` - 性能/内存基线脚本
- `perf-hotspot-optimization-v1` - 性能热点分析
- `perf-memory-leak-check-v1` - 内存泄漏检测

### 改动摘要

- **Tooltip 全局挂载**：在 `app.tscn` 添加全局 Tooltip，从 `battle.tscn` 移除重复实例。
- **art 字段修复**：移除 `shop_ui_view_model.gd`/`relic_potion_view_model.gd` 中对不存在的 `relic.art`/`potion.art` 的访问。
- **基线脚本修复**：
  - 重写 `memory_baseline.sh`/`soak_test.sh`，使用 macOS 兼容的 sed 替代 grep -P
  - 修复 GUT Orphan Reports 统计逻辑，避免误匹配测试名称
  - 移除 `|| true` 吞掉 Godot 失败的问题
- **文档口径修正**：
  - 更新 verification 文档中过时的指标描述（StringName 替代 Static Memory 等）
  - 更新测试数量 126→131
  - 补充缺失的 handoff.md/verification.md

### 验证结果

- [x] GUT 全量测试通过（131/131）
- [x] `bash dev/tools/memory_baseline.sh` 输出正确指标
- [x] 所有任务目录有完整的 handoff.md 和 verification.md

## 2026-02-20（phase7-8 审核收口门禁修复）

### 改动摘要

- 新建收口任务：`phase7-8-audit-closeout-v1`，用于处理多任务混合工作树下的 workflow 门禁闭环。
- 切换到合规分支：`chore/process-phase7-8-audit-closeout-v1`。
- 补齐 `fix-tooltip-event-signature-v1` 任务计划中的“改动白名单文件”段，修复 `workflow-check` 对空白名单的阻断。
- 完成收口任务三件套：
  - `docs/tasks/phase7-8-audit-closeout-v1/plan.md`
  - `docs/tasks/phase7-8-audit-closeout-v1/handoff.md`
  - `docs/tasks/phase7-8-audit-closeout-v1/verification.md`

### 验证结果

- [x] `make workflow-check TASK_ID=phase7-8-audit-closeout-v1` 通过
- [x] `HOME=/tmp bash dev/tools/run_gut_tests.sh 180` 通过（131/131）
- [x] `bash dev/tools/memory_baseline.sh` 输出 `GUT Orphan Reports: 0`

## 2026-02-20（worktree 收口与可提交性修复）

### 改动摘要

- 修复 `dev/tools/run_gut_tests.sh`：增加 HOME 可写兜底，支持默认 `make test` 直接运行。
- 更新 `.gitignore`：忽略 `dev/tools/baselines/*.txt`，降低高波动产物噪音。
- 修复任务文档一致性：将多份 `plan.md` 的状态改为 COMPLETED，并勾选验收项。
- 补齐 `feat-tooltip-extension-v1` 白名单文件，覆盖 `app.tscn` / `battle.tscn` 与相关 viewmodel。
- 新增任务三件套：`docs/tasks/chore-worktree-closeout-v1/`。

### 验证结果

- [x] `make test` 通过（自动回退 `/tmp` 后 GUT 131/131）
- [x] `make workflow-check TASK_ID=phase7-8-audit-closeout-v1` 通过

## 2026-02-20（Phase 7：P1 技术债清零 + Bug 修复）

### 规划文档修订

- 更新 `docs/后续开发规划v1.0.md`（V1.0 → V1.1）：
  - Phase 7 增加任务 7-6（营火升级逻辑统一）、7-7（性能基线验证）
  - Phase 7 增加执行批次说明（P1 串行、P2 并行）
  - Phase 7 增加性能指标（FPS>=45, Orphan=0, 内存<150MB）
  - Phase 8 拆分为 8-1（BuffSystem 重构）和 8-2（遗物扩展）
  - P1-3 从 Phase 12 提前至 Phase 8-1
  - P1-7 从 Phase 12 提前至 Phase 7-6

### 任务执行（批次 1：P1 阻断项）

#### 7-4：击杀敌人立即判定胜利（P1-2）

- 修改 `runtime/scenes/battle/battle.gd`：
  - `_on_enemy_died()`: 敌人死亡后立即调用 `check_battle_end()`
  - `_on_player_died()`: 玩家死亡立即触发失败，不再等待 RESOLVE 阶段
- 新增 GUT 测试 `test_battle_context.gd`：
  - `test_killing_all_enemies_triggers_immediate_victory()`
  - `test_player_death_triggers_immediate_defeat()`
  - `test_battle_continues_when_enemies_alive()`

#### 7-5：遗物触发对象缓存（P1-4）

- 已在前期实现：`relic_potion_system.gd` 使用 `_relic_runtimes` 字典缓存
- 已有 GUT 测试覆盖：`test_relic_runtime_cache_reuses_same_instance()`

#### 7-6：营火升级逻辑统一（P1-7）

- 已在前期实现：`run_state.gd:upgrade_card_in_deck_at()` 优先使用 `upgrade_to` 字段
- 已有集成测试覆盖：`test_rest_screen_upgrade_uses_upgrade_to_field()`

### 任务执行（批次 2：P2 体验修复）

#### 7-1：奖励界面英文序号修复

- 已在前期修复：viewmodel 使用 `card.get_display_name()` 显示中文名称
- 已有中文化提交：`feat-card-display-name-v1`

#### 7-2：遗物 Tooltip 不显示修复

- 修改 `runtime/modules/ui_shell/viewmodel/relic_potion_view_model.gd`：
  - 将 `relic_list_text` 改为 `relic_items` 数组结构
- 修改 `runtime/scenes/ui/relic_potion_ui.gd`：
  - 新增 `_render_relics()` 方法渲染遗物按钮
  - 新增 `_on_relic_button_mouse_entered/exited()` 连接 tooltip 信号
- 修改 `runtime/scenes/ui/relic_potion_ui.tscn`：
  - `RelicListLabel` 从 Label 改为 VBoxContainer

#### 7-3：出牌后卡牌卡屏修复

- 修改 `runtime/scenes/card_ui/card_ui.gd`：
  - `play()` 方法在所有提前返回路径上都调用 `queue_free()`

### 任务执行（批次 3：性能验证）

#### 7-7：性能基线验证

- 运行 `bash dev/tools/memory_baseline.sh`
- 结果：Tests 134/134, GUT Orphan Reports = 0

### 验证结果

- [x] `make test` 通过（GUT 134/134）
- [x] 性能基线达标（Orphan = 0）
- [x] P1 技术债清零（P1-2, P1-4, P1-7）

## 2026-02-20（Phase 7 审核修复）

### 审核发现的问题

| 级别 | 问题 |
|------|------|
| High | `phase7-8-audit-closeout-v1` 白名单未覆盖实际改动 |
| High | 4 个任务目录缺少三件套 |
| Medium | 性能基线脚本缺少 FPS/内存采集 |
| Medium | 7-2 缺少自动化回归测试 |
| Medium | 7-3 缺少自动化回归测试 |

### 修复内容

1. **白名单修复**：
   - 更新 `docs/tasks/phase7-8-audit-closeout-v1/plan.md`
   - 新增 `dev/tests/`、`docs/后续开发规划v1.0.md`、`docs/phase7_review_prompt.md`

2. **任务三件套补齐**：
   - `docs/tasks/fix-reward-chinese-ordinals-v1/` （已前期完成）
   - `docs/tasks/fix-relic-runtime-cache-v1/` （已前期完成）
   - `docs/tasks/fix-rest-upgrade-consistency-v1/` （已前期完成）
   - `docs/tasks/perf-baseline-validation-v1/` （部分达成）

3. **自动化测试补充**：
   - `test_relic_view_model_produces_tooltip_data()` - 7-2 回归测试
   - `test_relic_view_model_handles_empty_relics()` - 7-2 回归测试
   - `test_card_ui_play_calls_queue_free_on_success()` - 7-3 回归测试
   - `test_card_ui_play_calls_queue_free_on_insufficient_mana()` - 7-3 回归测试
   - `test_card_played_signal_emitted_on_play()` - 7-3 回归测试

### 验证结果

- [x] `make test` 通过（139/139）
- [x] `make workflow-check TASK_ID=phase7-8-audit-closeout-v1` 通过
- [ ] FPS/内存采集脚本待后续完善（Medium 技术债）

## 2026-02-20（Phase 9：力量轴卡牌扩展收口并合并主线）

### 改动摘要

- 完成并复审收口任务 `phase9-strength-axis-cards-v1`，将分支 `feat/card-phase9-strength-axis-cards-v1` 快进合并到 `main`。
- 卡牌扩展落地：战士卡池由 25 张扩展到 30 张，新增 5 张力量轴核心卡并纳入内容管线。
- 遗物扩展落地：新增战怒之戒、淬炼石；遗物字段解析与存档持久化补齐（`relic_catalog.gd` + `save_service.gd`）。
- 关键审核问题修复完成：
  - `lose_hp` 新增 `target_self`，修复血誓打击目标错误。
  - 去除手写卡运行时分支，统一走 JSON 导入与效果管线。
  - 卡牌 UI Tooltip 构建去重，避免名称重复显示。
  - 淬炼石触发点改为 `ON_BATTLE_START`，中途获取后下场战斗生效。

### 验证结果

- [x] `make test` 通过（142/142）
- [x] `make workflow-check TASK_ID=phase9-strength-axis-cards-v1` 通过（合并前在任务分支验证）
- [x] 主线当前基线提交：`f04ef84`

## 2026-02-21（fix-p0p2-mechanics-consistency-v1：P0/P2 机制一致性修复）

### 改动摘要

- 修复继续游戏模板恢复策略：
  - `SaveService.load_run_state()` 新增模板解析回调，读档时优先按存档 `character_id` 解析角色模板。
  - `RunLifecycleService.try_load_saved_run()` 接入 `CharacterRegistry` 解析回调。
  - `app.gd` 继续游戏不再预先按“当前选中角色”注入模板。
- 修复伤害药水战斗外误消耗：
  - `RelicPotionSystem._use_damage_potion()` 在无目标时不再调用 `_consume_potion()`。
- 修复遗物重复 ID 风险：
  - `RunState.add_relic()` 新增重复 ID 拒绝。
  - `SaveService._deserialize_relics()` 新增读档去重（保留首条）。
- 修复遗物 `draw_cards` 语义：
  - `RelicPotionSystem` 改为通过 `BattleContext.draw_cards()` 抽到手牌。
  - 无战斗上下文时跳过并发 warning，避免隐式写入弃牌堆。
- 补充测试覆盖：
  - `test_damage_all_enemies_potion_not_consumed_without_targets`
  - `test_relic_draw_cards_uses_battle_context_draw`
  - `test_run_state_add_relic_rejects_duplicate_id`
  - `test_resolve_base_stats_prefers_saved_character_template`
  - `test_resolve_base_stats_falls_back_when_resolver_invalid`
  - `test_deserialize_relics_deduplicates_same_id`

### 验证结果

- [x] `make test` 通过（147/147）
- [x] `make workflow-check TASK_ID=fix-p0p2-mechanics-consistency-v1` 通过
- [x] `bash dev/tools/save_load_replay_smoke.sh` 通过（9 组检查全部通过）
