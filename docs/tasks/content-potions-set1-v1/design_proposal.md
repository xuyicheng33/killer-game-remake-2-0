# 设计提案（内容填充简化）

## 任务 ID
`content-potions-set1-v1`

## 目标效果
- 新增 1 个可加载药水资源，使内容层药水总量达到 4。

## 非目标
- 不新增 `PotionData.EffectType`。
- 不改动 `RelicPotionSystem` 对药水的执行逻辑。

## 方案
- 方案 A（采用）：按既有 `PotionData` 字段（HEAL/GOLD/BLOCK）新增资源条目。
- 方案 B（不采用）：扩展“力量药水/伤害药水”等新效果类型；该方案属于机制扩展，超出本任务。

## 兼容性影响
- 存档：无结构变化。
- 种子一致性：无随机规则变化，仅扩展内容池。
