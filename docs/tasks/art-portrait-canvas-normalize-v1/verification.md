# 验证记录

## 基本信息

- 任务 ID：`art-portrait-canvas-normalize-v1`
- 日期：2026-02-16

## 自动化检查

- [x] `make workflow-check TASK_ID=art-portrait-canvas-normalize-v1`
  - 结果：`[workflow-check] passed.`

## Godot 4.6 CLI 自检

- [x] `godot4.6 --version`
  - 结果：`4.6.stable.mono.official.89cea1439`
- [ ] `godot4.6 --headless --path /Users/xuyicheng/杀戮游戏复刻2.0 --quit`
  - 结果：约 40 秒未退出，手动 `Ctrl+C` 终止
  - 日志：
    - `Godot Engine v4.6.stable.mono.official.89cea1439 - https://godotengine.org`
    - `Error received in message reply handler: Connection invalid`
    - `Connection Invalid error for service com.apple.hiservices-xpcservice.`
  - 挂起处理：若未正常退出，记录日志并说明环境问题（不增加自动退出逻辑）

## 功能验证（可复验步骤）

### 主路径 1：三敌同场时立绘不明显重叠，主要轮廓可分辨

- 步骤：
  1. 使用 processed 立绘进入战斗场景（含三敌）。
  2. 观察敌人间横向占位与轮廓区分度。
  3. 记录敌人纹理尺寸、最终 scale、显示宽度。
- 预期：三敌立绘无明显横向挤压重叠，轮廓可分辨。
- 实测：静态验证通过（当前环境未完成可视化实跑）。
  - processed 贴图均为 `2048x2048`，可见轮廓宽度为 `1638px`。
  - 敌人最终采用宽度约束：`MAX_PORTRAIT_WIDTH = 220px`，两种敌人最终显示宽均为 `220px`。
  - 三敌横向中心距（战斗场景）：
    - `1305 -> 1538`：距离 `233`，间隙约 `13px`
    - `1538 -> 1755`：距离 `217`，理论重叠约 `3px`（轻微，属“非明显重叠”）

### 主路径 2：敌人箭头位置贴近人物，不漂移

- 步骤：
  1. 战斗中触发鼠标悬浮敌人，显示箭头。
  2. 观察箭头与人物右侧轮廓的距离。
  3. 对比改前基于整图宽度时的偏移。
- 预期：箭头贴近人物轮廓，距离稳定，无明显远离。
- 实测：静态数值验证通过（当前环境未完成可视化实跑）。
  - 改前（高度缩放，仅按整图宽）：
    - 蝙蝠（离恨烟原图）：显示宽约 `620.107px`，箭头偏移约 `315.054px`
    - 螃蟹（埋骨钱原图）：显示宽约 `707.112px`，箭头偏移约 `358.556px`
  - 改后（基于可见轮廓 + 宽度约束）：
    - 两类敌人显示宽均 `220px`
    - 箭头偏移统一约 `115px`（`220 / 2 + 5`）
  - 结果：箭头明显更贴近人物轮廓，不再远漂。

### 边界 1：切回旧 16x16 资源时缩放逻辑不崩

- 步骤：
  1. 临时切回低分辨率旧图引用（本地验证后恢复）。
  2. 进入战斗观察玩家/敌人缩放表现。
  3. 恢复 processed 引用。
- 预期：低分辨率资源保持默认缩放逻辑，不崩溃。
- 实测：逻辑验证通过（按阈值分支计算）。
  - 玩家旧图 `16x16`：保留默认 `scale = 5.5`，显示 `88x88`
  - 敌人旧图 `16x16`：保留默认 `scale = 5.0`，显示 `80x80`
  - 低分辨率分支未走宽度约束，兼容旧资源表现。

## 改前/改后关键数值

- 玩家（`霜北刀`）：
  - 改前（原图 + 高度缩放）：`texture=8120x4134`，`scale=0.101597`，显示 `824.964x420.000`
  - 改后（processed + 高宽双约束）：`canvas=2048x2048`，可见 `1638x843`，`scale=0.195360`，显示 `320.000x164.689`
- 敌人（`离恨烟`）：
  - 改前（原图 + 高度缩放）：`texture=8995x5222`，`scale=0.068939`，显示 `620.107x360.000`
  - 改后（processed + 高宽双约束）：`canvas=2048x2048`，可见 `1638x1259`，`scale=0.134310`，显示 `220.000x169.096`
- 敌人（`埋骨钱`）：
  - 改前（原图 + 高度缩放）：`texture=8120x4134`，`scale=0.087083`，显示 `707.112x360.000`
  - 改后（processed + 高宽双约束）：`canvas=2048x2048`，可见 `1638x745`，`scale=0.134310`，显示 `220.000x100.061`
