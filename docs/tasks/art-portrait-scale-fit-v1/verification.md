# 验证记录

## 基本信息

- 任务 ID：`art-portrait-scale-fit-v1`
- 日期：2026-02-16

## 自动化检查

- [x] `make workflow-check TASK_ID=art-portrait-scale-fit-v1`
  - 结果：`[workflow-check] passed.`

## Godot 4.6 CLI 自检

- [x] `godot4.6 --version`
  - 结果：`4.6.stable.mono.official.89cea1439`
- [ ] `godot4.6 --headless --path /Users/xuyicheng/杀戮游戏复刻2.0 --quit`
  - 结果：约 40 秒未退出，手动 `Ctrl+C` 终止
  - 日志：
    - `Godot Engine v4.6.stable.mono.official.89cea1439 - https://godotengine.org`
    - `Error received in message reply handler: Connection invalid`
    - `Connection Invalid error for service com.apple.hiservices-xpcservice.`
  - 挂起处理：若未正常退出，记录日志并说明环境问题（不增加自动退出逻辑）

## 功能验证（可复验步骤）

### 主路径 1：进入战斗后玩家立绘完整可见，不超出主要可视区域，不遮挡手牌区

- 步骤：
  1. 启动战斗场景，观察玩家立绘在左下区域的占位。
  2. 核对玩家立绘底部不侵入手牌主要显示区域。
  3. 记录玩家贴图尺寸与最终缩放值。
- 预期：玩家立绘完整可见、不过屏、不遮挡手牌区。
- 实测：静态验证通过（当前环境未完成可视化实跑）。
  - 玩家高分辨率立绘：`4134px` 高，最终 scale=`0.101597`，显示高度=`420px`。
  - 战斗场景玩家坐标 `y=600`，推导显示底边约 `810`（`600 + 420/2`）。
  - 手牌容器顶部约 `820`（由 `scenes/battle/battle.tscn` 锚点与偏移推导），未侵入主要手牌区。

### 主路径 2：进入战斗后敌人立绘完整可见，意图与状态 UI 可正常显示

- 步骤：
  1. 启动战斗场景，观察三名敌人的立绘占位。
  2. 核对敌人意图 UI 与状态 UI 未被立绘遮挡。
  3. 记录敌人贴图尺寸与最终缩放值。
- 预期：敌人立绘完整可见，意图/状态 UI 显示正常。
- 实测：静态验证通过（当前环境未完成可视化实跑）。
  - 敌人高分辨率立绘（埋骨钱）：`4134px` 高，scale=`0.087083`，显示高度=`360px`。
  - 敌人高分辨率立绘（离恨烟）：`5222px` 高，scale=`0.068939`，显示高度=`360px`。
  - 敌人意图与状态 UI 节点仍为原有子节点层级与偏移，显示链路未改。

### 边界用例 1：切回低分辨率旧图时缩放不异常、不会崩溃

- 步骤：
  1. 临时将角色/敌人 art 引用切回旧 `16x16` 图（仅本地验证，不保留该状态）。
  2. 进入战斗，观察显示比例是否保持旧表现。
  3. 恢复 art 引用。
- 预期：低分辨率旧图不被异常放大/缩小，运行稳定不崩溃。
- 实测：逻辑验证通过（按阈值分支计算）。
  - 玩家旧图 `16px` 高，命中低分辨率分支，保留默认 scale=`5.5`，显示高度=`88px`。
  - 敌人旧图 `16px` 高，命中低分辨率分支，保留默认 scale=`5.0`，显示高度=`80px`。
  - 未引入新节点或流程逻辑，不涉及崩溃风险放大点。

## 改前/改后关键数值

- 玩家（高分图 `霜北刀`）：
  - 改前：`texture_h=4134`，`scale=5.5`，显示高度约 `22737`
  - 改后：`texture_h=4134`，目标高度 `420`，`scale=0.101597`，显示高度 `420`
- 敌人（高分图 `埋骨钱`）：
  - 改前：`texture_h=4134`，`scale=5.0`，显示高度约 `20670`
  - 改后：`texture_h=4134`，目标高度 `360`，`scale=0.087083`，显示高度 `360`
- 敌人（高分图 `离恨烟`）：
  - 改前：`texture_h=5222`，`scale=5.0`，显示高度约 `26110`
  - 改后：`texture_h=5222`，目标高度 `360`，`scale=0.068939`，显示高度 `360`
- 低分辨率兼容（旧 `16x16`）：
  - 玩家：保留默认 `scale=5.5`
  - 敌人：保留默认 `scale=5.0`
