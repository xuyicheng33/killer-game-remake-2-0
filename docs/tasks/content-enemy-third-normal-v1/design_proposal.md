# 设计提案（内容填充简化）

## 任务 ID
`content-enemy-third-normal-v1`

## 目标效果
- 新增普通敌人 `viper`（施毒高权重）。
- 完成内容层资源与数据定义，并完成运行时注册接线。

## 非目标
- 不改 `EnemyIntentRules` 核心选取算法。
- 不新增新的状态类型。

## 方案
- 方案 A（采用）：新增敌人资源 + 新动作脚本 + 数据源条目 + 注册表与遭遇接线。
- 方案 B（不采用）：仅补内容不接线；会导致地图普通战斗无法出现新敌人。

## 兼容性影响
- 存档：无字段变化。
- 种子一致性：不改 RNG 算法。
