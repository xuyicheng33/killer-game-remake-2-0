# 设计提案（内容填充简化）

## 任务 ID
`content-relics-set1-v1`

## 目标效果
- 新增 2 个遗物资源，使内容层达到 6 个遗物。
- 覆盖回合开始格挡与击杀奖励两类已有触发字段。

## 非目标
- 不新增遗物字段。
- 不改动 `RelicPotionSystem` 触发逻辑。

## 方案
- 方案 A（采用）：按 `RelicData` 既有导出字段直接补充 `.tres` 资源。
- 方案 B（不采用）：扩展遗物 schema + 导入器；该方案超出“内容填充”范围。

## 兼容性影响
- 存档：无字段变更。
- 种子一致性：无算法变化，仅内容池扩展。
