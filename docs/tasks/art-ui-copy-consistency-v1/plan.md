# 任务计划

## 基本信息

- 任务 ID：`art-ui-copy-consistency-v1`
- 任务级别：`L1`
- 目标阶段：`D4（中文文案与界面一致性打磨 v1）`
- 负责人：Codex
- 日期：2026-02-16

## 级别判定（L1）与理由

- 本任务仅涉及中文文案统一与 UI 可读性参数微调（换行、最小尺寸、间距）。
- 不改战斗规则、数值、存档、种子、流程逻辑，也不涉及音频替换或音频逻辑。
- 改动范围集中在场景文案与 UI 脚本显示文本，属于 `L1`。

## 目标（最小可用）

1. 统一关键术语：
   - 生命、格挡、金币、回合、阶段、结束回合
   - 抽牌堆、手牌、弃牌堆、消耗堆
   - 奖励、跳过、休息、升级、商店、事件
2. 统一按钮与提示语风格（简洁、动作导向，避免同义混用）。
3. 修复明显可读性问题（长文本换行、关键标签不截断、小窗口关键文本可读）。
4. 不改玩法逻辑，仅改文案和最小 UI 参数。

## 执行约束

- 兼容 Godot 4.6 语法与资源格式。
- 禁止加入 headless 自动退出等运行时副作用逻辑。
- 不自动提交 git commit。

## 改动白名单文件（严格）

- `scenes/app/**`
- `scenes/battle/**`
- `scenes/map/**`
- `scenes/reward/**`
- `scenes/events/**`
- `scenes/shop/**`
- `scenes/ui/**`
- `main_theme.tres`
- `docs/tasks/art-ui-copy-consistency-v1/**`

## 明确不做

- 不做 D3 音频相关任何内容
- 不改 `modules/**` 业务逻辑
- 不改 `characters/**`、`enemies/**` 数值/行为
- 不做无关重构

## 实施步骤

1. 梳理白名单内文案与术语分布，建立本轮术语统一口径。
2. 统一地图、战斗、奖励、事件、商店、通用 UI 的按钮文案和提示语风格。
3. 修复长文本可读性问题（自动换行、最小宽度、间距等最小参数）。
4. 复核术语一致性并生成“改前 -> 改后”对照表。
5. 执行命令验证并回填三件套。

## 验证方案

1. `make workflow-check TASK_ID=art-ui-copy-consistency-v1`
2. `godot4.6 --version`
3. `godot4.6 --headless --path /Users/xuyicheng/杀戮游戏复刻2.0 --quit`（若挂起，记录日志与环境说明）
4. 用例验证：
   - 主路径 1：地图 -> 战斗 -> 奖励，术语与按钮文案一致
   - 主路径 2：休息点/商店/事件文案风格一致，长文本可读
   - 边界用例 1：小窗口或长文本下关键文本不消失、可读
5. 在 `verification.md` 附术语对照表（改前 -> 改后）。

