# 设计提案（内容填充简化）

## 任务 ID
`content-cards-warrior-set2-v1`

## 目标效果

- 卡牌总数达到 20。
- 满足 Phase 3b 类型与关键词分布。
- 卡面语义与运行时行为一致。

## 非目标

- 不在本任务内实现新机制代码（新机制拆分独立机制任务）。

## 方案

- 方案 A（采用）：扩容 `warrior_cards.json` + 导入生成资源。
- 依赖机制任务：
  - `feat-card-draw-energy-ops-v1`（抽牌/回能量）
  - `feat-card-exhaust-upgrade-on-consume-v1`（消耗后升级）
- 方案 B（不采用）：仅做内容扩容，不补机制；会导致语义验收被驳回。

## 兼容性影响

- 存档：无结构变化（机制任务单独处理字段兼容）。
- 种子一致性：卡池扩容不改变 RNG 算法。
