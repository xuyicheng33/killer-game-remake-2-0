# 任务计划

## 基本信息

- 任务 ID：`feat-buff-system-core-v1`
- 任务级别：`L2`
- 主模块：`buff_system`
- 负责人：AI 协作执行
- 日期：2026-02-15

## 目标

仅实现 A4 目标：建立状态容器、接入触发时机（回合开始/结束、受击、出牌后），并完成力量/敏捷/易伤/虚弱/中毒的最小可用结算与可视化。

## 范围边界

- 包含：
  - `buff_system` 状态容器与增减层接口。
  - 状态触发时机：回合开始、回合结束、受击、出牌后。
  - 五个状态：力量、敏捷、易伤、虚弱、中毒。
  - UI 状态图标与层数显示（最小可视化）。
  - 与现有 `effects` 结算联动（仅实现 A4 所需联动）。
- 不包含：
  - 卡牌关键词重构（A2）。
  - 敌人意图规则（A5）。
  - 奖励流程与 B 阶段任务。

## 改动白名单文件

- `docs/tasks/feat-buff-system-core-v1/**`
- `modules/buff_system/**`
- `effects/**`
- `scenes/ui/**`
- `custom_resources/**`
- `docs/contracts/battle_state.md`

## 实施步骤

1. 在 `modules/buff_system` 建立状态容器与统一读写接口（增减层、清理、查询）。
2. 定义五个核心状态数据结构及其触发时机钩子。
3. 将 A4 需要的数值修正接入现有效果结算链（不扩展至 A2/A5）。
4. 在 `scenes/ui` 增加状态图标与层数显示，保证玩家/敌人可观察。
5. 更新 `docs/contracts/battle_state.md` 中状态字段约定与触发语义。
6. 回填 `verification.md` 与 `handoff.md`。

## 验证方案

1. 执行 `make workflow-check TASK_ID=feat-buff-system-core-v1`，确认白名单通过。
2. 主路径：施加虚弱后，观察敌人伤害变化可观测且与期望一致。
3. 主路径：中毒在回合时机触发并正确结算层数变化。
4. 边界用例：状态层数在 0 或空状态时不出现负层/崩溃，UI 不显示非法值。

## 风险与回滚

- 风险：状态触发时机与现有阶段机/效果队列衔接错误，可能导致重复触发或漏触发。
- 回滚方式：按任务提交回滚；或回退白名单代码文件并恢复契约文档。
